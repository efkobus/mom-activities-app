import React from 'react';
import { Typography, Box } from '@mui/material';

const PackDetailPage = () => {
  return (
    <Box>
      <Typography variant="h4">Pack Details</Typography>
    </Box>
  );
};

export default PackDetailPage;