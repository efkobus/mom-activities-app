import React from 'react';
import { Typography, Box } from '@mui/material';

const ActivityPacksPage = () => {
  return (
    <Box>
      <Typography variant="h4">Activity Packs</Typography>
    </Box>
  );
};

export default ActivityPacksPage;